# requirements.txt — v2 数据/验证核心（2026-09-17 精简；旧下载器依赖随管线退役）
pandas>=2.2        # merge_asof/timestamp 语义（TP13 预注册时钉精确版本）
numpy>=1.26
pyarrow>=14        # parquet 读写

# 验证机附加（conda env "kronos" 已含）：
#   sympy, mpmath  -> research/scripts/verify_proposition_C.py
#   torch          -> kronos_context_adapter / audit_kronos_x10（本地权重，HF_HUB_OFFLINE=1）
